name: Deploy Edge Functions

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy all 32 Edge Functions
        run: |
          REF="${{ secrets.SUPABASE_PROJECT_ID }}"

          FUNCTIONS=(
            admin-morning-report
            auto-approve-check
            auto-remind
            bankid
            bankid-verify
            bankid-webhook
            booking-cancel-v2
            booking-create
            claude
            cleaner-job-match
            cleaner-optout
            cleanup-stale
            email-inbound
            geo
            get-cleaner-contact
            health
            notify
            notify-new-application
            onboarding-reminders
            onboarding-save
            places-autocomplete
            push
            referral-register
            rut-claim
            sms
            social-media
            stripe-connect
            stripe-refund
            stripe-webhook
            swish
            team-sms-notify
          )

          FAILED=0
          for fn in "${FUNCTIONS[@]}"; do
            echo "--- Deploying $fn ---"
            if supabase functions deploy "$fn" --project-ref "$REF" --no-verify-jwt; then
              echo "OK: $fn"
            else
              echo "FAILED: $fn"
              FAILED=$((FAILED + 1))
            fi
          done

          echo "--- Done: ${#FUNCTIONS[@]} functions, $FAILED failed ---"
          [ "$FAILED" -eq 0 ] || exit 1
