# §2.7.4 Manual test-plan

**Scope:** generate-receipt FAKTURA-rendering för B2B + F-serien + dubbel-mejl + serve-invoice F-regex

**Filer:** [`supabase/functions/generate-receipt/index.ts`](../../supabase/functions/generate-receipt/index.ts), [`supabase/functions/serve-invoice/index.ts`](../../supabase/functions/serve-invoice/index.ts)

**Deploy:** auto via GitHub Actions på push till `main`. Verifiera Actions-status innan test.

**Uppdatering 2026-04-23 (fix1):** Webbversion-länken är **avvecklad** i mejl-mallarna pga Supabase Edge Runtime CSP-sandbox som blockerar HTML-rendering från user Edge Functions. Kvittot/fakturan levereras nu **enbart i mejlet** (all information finns där). `receipt_url` sparas fortfarande i DB för framtida bruk (custom domain, PDF-generering — hygien #45). Test 8 (serve-invoice F-regex webbversion) är därmed **inte längre relevant** för E2E-flödet.

---

## Pre-test checklist

- [ ] Commit pushad
- [ ] GitHub Actions "Deploy Edge Functions" klar + grön
- [ ] `generate-receipt` + `serve-invoice` båda i listan:
  ```powershell
  npx supabase functions list --project-ref urjeijcncsyuletprydy |
    Select-String "generate-receipt|serve-invoice"
  ```
- [ ] §2.7.3 redan i prod (commit `de5493e`)

## Test 1 — B2C privat med RUT (regression)

**Syfte:** verifiera att B2C-kvittot fortfarande fungerar oförändrat efter §2.7.4.

1. `boka.html` → privat → välj Hemstädning med RUT
2. Fyll fullt + submit → betala via Stripe
3. Verifiera i inkorgen (`farrehagge@gmail.com`):
   - [ ] Subject: `Bokningsbekräftelse + kvitto — Hemstädning <datum>`
   - [ ] Mejlet har KVITTO-titel
   - [ ] Kvittonummer: `KV-2026-NNNNN`
   - [ ] RUT-avdrag-rad visas
   - [ ] "Visa min bokning →"-knapp (magic-link) — INGEN "Öppna som webbversion" (fix1)

4. Studio SQL:
```sql
SELECT id, customer_type, receipt_number, invoice_number, receipt_url, receipt_email_sent_at
FROM bookings
WHERE customer_email = 'farrehagge@gmail.com'
ORDER BY created_at DESC LIMIT 1;
```
**Förväntat:** `customer_type='privat'`, `receipt_number='KV-...'`, `invoice_number=NULL`, `receipt_url` finns, `receipt_email_sent_at` satt.

5. Klicka webbversion-länken → HTML öppnas med "KVITTO"-titel.

**Godkänt om:** allt ser likadant ut som §2.5-R2-kvitto (ingen regression).

## Test 2 — B2C privat utan RUT (regression)

1. `boka.html` → privat → **avaktivera RUT-toggle**
2. Submit → betala
3. Verifiera mejl: KVITTO utan RUT-rad, bara "Arbetskostnad exkl moms" + "Moms 25%" + "Att betala inkl moms"

**Godkänt om:** ingen RUT-rad visas, allt annat som Test 1.

## Test 3 — B2B minimal (bara obligatoriska fält)

1. `boka.html?type=foretag`
2. Fyll:
   - Företagsnamn: `Acme Test AB`
   - Org.nr: `556677-8899`
3. Lämna alla 7 valfria B2B-fält tomma (momsreg, kontaktperson, faktura-email, fakturaadress-checkbox ej markerad, referens)
4. Submit → betala

5. **Mejl-verifikation** (inkorgen):
   - [ ] Subject: `Faktura F-2026-NNNNN — Kontorsstädning <datum>` (INTE "Bokningsbekräftelse + kvitto")
   - [ ] Mejlet har FAKTURA-titel (inte KVITTO)
   - [ ] Fakturanummer: `F-2026-NNNNN` (inte KV-)
   - [ ] Betalningsstatus-box: "✓ Betald via kortbetalning den <datum>"
   - [ ] "Köpare"-sektion visar: företagsnamn + org.nr + fakturaadress (= tjänsteadress eftersom checkbox ej markerad)
   - [ ] **INGEN RUT-rad** i belopp-sektionen
   - [ ] Belopp: Arbetskostnad exkl moms / Moms 25% / Att betala inkl moms
   - [ ] Spick-plattform-notering: "Fakturan genererades av Spick, plattformen för städtjänster. Arbetet utfördes av <cleaner-namn>."
   - [ ] "Visa bokning →"-knapp (magic-link) — INGEN "Öppna fakturan som webbversion" (fix1)

6. Studio SQL:
```sql
SELECT id, customer_type, receipt_number, invoice_number, receipt_url,
       business_name, business_vat_number, business_contact_person,
       business_invoice_email, invoice_address_street
FROM bookings WHERE customer_type='foretag'
ORDER BY created_at DESC LIMIT 1;
```
**Förväntat:**
- `customer_type='foretag'`
- `invoice_number='F-2026-NNNNN'`
- `receipt_number=NULL` (mutually exclusive)
- `receipt_url` finns
- `business_name='Acme Test AB'`, `business_vat_number=NULL`, `business_contact_person=NULL`, `business_invoice_email=NULL`, `invoice_address_street=NULL`

## Test 4 — B2B full (alla 10 fält)

1. `boka.html?type=foretag`
2. Fyll:
   - Företagsnamn: `Acme Test AB`
   - Org.nr: `556677-8899`
   - Momsreg.nr: `SE559402452201`
   - Kontaktperson: `Anna Andersson`
   - Faktura-e-post: **SAMMA** som customer_email (för Test 4 — dubbel-mejl testas i Test 5)
   - Checkbox fakturaadress: markera
   - Fakturaadress: `Kungsgatan 10`, `171 64`, `Solna`
   - Referens: `PO-2026-0123`
3. Submit → betala

4. **Mejl-verifikation:**
   - Samma som Test 3 plus:
   - [ ] Köpare-sektion visar `Momsreg.nr: SE559402452201`
   - [ ] Köpare-sektion visar `Att: Anna Andersson`
   - [ ] Köpare-sektion visar fakturaadress `Kungsgatan 10 / 171 64 Solna` (**inte** customer_address)
   - [ ] Köpare-sektion visar `Referens: PO-2026-0123`

5. Studio SQL: alla 10 kolumner populerade korrekt.

## Test 5 — Dubbel-mejl (B2B med separat business_invoice_email)

**Syfte:** verifiera E1-logik (dubbel-mejl bara om emails skiljer sig).

1. `boka.html?type=foretag` — kund-email: `farrehagge@gmail.com`
2. Fyll `business_invoice_email: hello@spick.se` (annan inkorg du har åtkomst till)
3. Fyll resten minimalt (företagsnamn + org.nr)
4. Submit → betala

5. **Mejl-verifikation:**
   - [ ] `farrehagge@gmail.com` fick ETT mejl med FAKTURA
   - [ ] `hello@spick.se` fick OCKSÅ ETT mejl med FAKTURA (samma innehåll)
   - Båda ska ha samma subject + samma F-nummer

6. **Log-verifikation** (Supabase Dashboard → Edge Functions → generate-receipt → Logs):
```
[RECEIPT] Invoice email sent to customer farrehagge@gmail.com id: ...
[RECEIPT] Invoice email also sent to hello@spick.se id: ...
[RECEIPT] B2B invoice generated: {
  booking_id: ...,
  invoice_number: F-2026-NNNNN,
  has_separate_invoice_email: true,
  emails_sent_count: 2,
  email_ok: true
}
```

**GDPR-skydd:** logga `has_separate_invoice_email: true`, men **INTE** det faktiska email-värdet.

## Test 6 — Fakturaadress-fallback (E2)

**Syfte:** verifiera att `invoice_address_street=NULL` → fakturan visar tjänsteadress.

1. `boka.html?type=foretag` → tjänsteadress `Testgatan 5`
2. Fyll B2B-data men **lämna fakturaadress-checkbox omarkerad**
3. Submit → betala

4. Öppna fakturan (webbversion):
   - Köpare-sektion, "Fakturaadress"-fältet: visar `Testgatan 5` (= tjänsteadress)

**Godkänt om:** ingen "Adress saknas"-text, fallback till customer_address fungerar.

## Test 7 — Idempotens B2B (E3 — INGEN regenerering av F-nummer)

**Syfte:** verifiera att re-run av generate-receipt INTE förbrukar nytt sekvens-värde.

1. Gör en B2B-bokning (Test 3 eller 4), vänta tills mejl kommit.
2. Notera `invoice_number` från Studio (t.ex. `F-2026-00003`).
3. Trigga generate-receipt manuellt:

```powershell
$body = @{ booking_id = "<boking-id>" } | ConvertTo-Json
Invoke-RestMethod `
  -Uri "https://urjeijcncsyuletprydy.supabase.co/functions/v1/generate-receipt" `
  -Method POST -Body $body -ContentType "application/json" `
  -Headers @{ apikey = $env:SUPABASE_ANON_KEY; Authorization = "Bearer $env:SUPABASE_ANON_KEY" }
```

4. Förväntad response:
```json
{
  "success": true,
  "already_sent": true,
  "is_b2b": true,
  "document_number": "F-2026-00003",
  "receipt_url": "..."
}
```

5. Studio SQL: `invoice_number` oförändrat (fortfarande `F-2026-00003`).
6. Nästa verkliga B2B-bokning: sekvensen **ska** vara `F-2026-00004` (inte `00005`).

**Godkänt om:** ingen nytt nummer genererat, ingen sekvens-läckage.

## Test 8 — [AVVECKLAD av fix1]

**Ursprunglig syfte:** verifiera serve-invoice regex för F-prefix-rendering i webbläsaren.

**Status efter fix1 (2026-04-23):** Webbversion-länken är borttagen från mejlen pga Supabase Edge Runtime CSP-sandbox. Serve-invoice-EF:n är fortfarande aktiv och har F-regex-fixen (B-14) kvar — men **kunden ser aldrig länken i mejlet**. Kvittot/fakturan finns komplett i mejl-innehållet.

**Om du ändå vill verifiera serve-invoice**:
- Serve-invoice kan fortfarande anropas direkt (t.ex. för admin/intern debug):
  ```
  https://urjeijcncsyuletprydy.supabase.co/functions/v1/serve-invoice?file=F-2026-NNNNN.html
  ```
- Men pga CSP-sandbox kommer browsers att visa HTML-källkod, inte renderad sida. Inte ett buggprogram — plattformsbegränsning.
- Regex-validering fungerar: `BADPREFIX-...` → 400, `F-...`/`KV-...`/`SF-...` → download OK.

**Framtida lösning:** custom domain (kvitto.spick.se) eller PDF-generering — hygien #45.

## Test 9 — Notifikation vid mejl-fel (B-17)

**Syfte:** verifiera B2B-specifik terminologi i admin-fel-mejl.

Detta är svårt att trigga avsiktligt. Alternativ:
- Temporärt sätta fel Resend-API-key (riskabelt, gör inte i prod)
- Alternativt: verifiera via kod-inspektion att `notifyAdminEmailFailure(bookingId, email, err, true)` används för B2B (`isB2B=true` skickas)

I admin-mejlet (om det utlöses):
- [ ] Subject: `⚠️ Fakturamejl misslyckades — bokning <prefix>` (inte "Kvittomejl")
- [ ] Body säger "generate-receipt kunde inte skicka fakturan" (inte "kvittot")
- [ ] Visar `Dokumenttyp: B2B-faktura (F-serie)` i kortet

**Verifierat via kod-inspektion:** [`generate-receipt/index.ts:notifyAdminEmailFailure()`](../../supabase/functions/generate-receipt/index.ts) har `isB2B`-parameter och använder den konsekvent.

---

## Rapportering efter test

Klistra i chat:
- **Test 1-9:** ✅/🔴
- **Studio SELECT** från Test 3 och Test 4 (kopiera rader)
- **Edge Function log-rad** från Test 5 (dubbel-mejl)
- **Screenshot** av fakturan (webbversion) från Test 4 — visuellt check

---

## Scope-expansion-noteringar

**Ändringar inom scope:**
- ✅ generate-receipt B2B-rendering + F-serien + dubbel-mejl
- ✅ serve-invoice 1-rad regex-fix (inom B-14-beslut)

**Oförändrat (scope-respekt):**
- booking-create (§2.7.3)
- boka.html (§2.7.2)
- DB-schema (§2.7.1)
- Pricing-logik (§2.6)
- faktura.html (§2.5-R4)

**Kvarstående hygien-tasks:**
- #44 (ny): serve-invoice behöver fullständig refaktor i §2.7.5

---

## Kända begränsningar

1. **Inga automated tests** för de nya B2B-mallarna. Samma hygien-task #43 (booking-create + nu även generate-receipt B2B). Mall-duplikation mot test-filer gjorde inga test-uppdateringar möjliga utan refaktor.
2. **Deno type-check visar 4 errors** som är PRE-EXISTING (verifierat via `git stash`). Kopplat till Supabase-client-typegenerics, inte §2.7.4. Supabase-runtime kör fint.
3. **Serve-invoice saknar CSP-headers** — hygien #44 för §2.7.5.
