# ═══════════════════════════════════════════════════
# SPICK .gitignore
# ═══════════════════════════════════════════════════

# Känslig data — ALDRIG i git
SESSION_HANDOFF.md
backups/
*.env
.env.*
secrets.json

# Byggfiler
node_modules/
dist/
.cache/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.vscode/
.idea/
.claude/

# Lokal CLI-state + backups (aldrig i git)
supabase/.temp/
*.bak
*.local

# Temporärt
*.log
*.tmp
node_modules/
test-results/
playwright-report/

# Lokal automationsagent — körs lokalt, inte en del av webbplatsen
agent/

# Lokala DB-backups — innehåller PII, ska aldrig committas
backups/
fix*.js
search-dashboard.js

# Local DB schema dumps - never commit
prod-schema.sql

# Claude Code local config
.agents/
skills-lock.json

# k6 load-test results (ephemeral, regenereras per körning)
tests/load/latest-result.json
tests/load/summary.json

# Supabase CLI branch-state (ej i git per default)
supabase/.branches/
